// config.js
// NEUTRALIZADO (legacy). NO usar para API.
// Se deja solo por compatibilidad histórica.

export const CONFIG = {
  APP_NAME: "I GUIDE U",
  DEFAULT_LANGUAGE: "en",
  SUPPORTED_LANGUAGES: ["en", "es"],
  TIMEOUT: 15000,
};
